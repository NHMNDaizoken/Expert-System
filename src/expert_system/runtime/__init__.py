"""
runtime — Session state, diagnosis results, and reasoning trace models.

Submodules:
  state   — WorkingMemory for managing diagnosis session state.
  result  — DiagnosisResponse and related typed data models.
  trace   — ExplanationBuilder for human/machine-readable reasoning traces.
"""
